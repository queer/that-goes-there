---
hosts:
  localhost:
    host: "10.42.0.69"
    executor: "ssh"
    remote_user: "amy"
  ssh-localhost:
    host: "localhost"
    executor: "ssh"
    remote_user: "amy"

groups:
  ssh-group:
    - "localhost"
    - "ssh-localhost"
